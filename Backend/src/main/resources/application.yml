spring:
  application:
    name: Backend
  datasource:
    url: jdbc:mysql://localhost:3306/gm
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    properties:
      hibernate:
        format_sql: true


